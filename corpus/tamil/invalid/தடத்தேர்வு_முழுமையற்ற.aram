// invalid: unclosed தடத்தேர்வு (EOF mid-construct; must terminate)
தொகுப்பு தொடக்கம்

செயல்பாடு தொடக்கம்() {
    ச := ஆக்கு(தடம் முழுஎண், 1)
    தடத்தேர்வு {
        எனில் ம := <-ச {
            பதிப்பி(ம)
        
}
