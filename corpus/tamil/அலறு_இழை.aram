// Aram: அலறு_இழை.aram — recover in இழை does not leak to தொடக்கம்
தொகுப்பு தொடக்கம்

செயல்பாடு தொடக்கம்() {
    ச := ஆக்கு(தடம் சரம்)
    இழை செயல்பாடு() {
        தள்ளிவை செயல்பாடு() {
            ம := மீள்()
            ச <- ம
        }()
        அலறு("இழை")
    }()
    பதிப்பி(<-ச)
}
